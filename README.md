## Instructions
have react & npm installed & install dependencies
### Linux
```bash
sudo apt install node npm
# start frontend
cd /path/to/my-book-haven-93
npm install
npm run dev
# start backend
cd /path/to/backend
npm install && node server.js
```
### Windows
download from official website & download dependencies & start server

